import { CapsonDirective } from './capson.directive';

describe('CapsonDirective', () => {
  it('should create an instance', () => {
    const directive = new CapsonDirective();
    expect(directive).toBeTruthy();
  });
});
